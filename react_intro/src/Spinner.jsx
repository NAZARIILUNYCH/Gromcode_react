/* eslint-disable no-unused-expressions */
import React from 'react';

const Spinner = props => (
  <span style={{ height: props.size, width: props.size }} className="spinner"></span>
);

export default Spinner;
